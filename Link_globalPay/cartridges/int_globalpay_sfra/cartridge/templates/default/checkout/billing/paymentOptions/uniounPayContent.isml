<div class="tab-pane uniounpay-content" id="uniounpay-content" role="tabpanel">
    <fieldset class="payment-form-fields">

        <!--- payment method is unionpay --->
        <input type="hidden" class="form-control"
               name="${pdict.forms.billingForm.paymentMethod.htmlName}"
               value="${Resource.msg('paymentmethodname.unionpay', 'globalpay', null)}"
        >
<fieldset class="uniounPay-form">
<isif condition=${pdict.isSandbox=='true'}>
    <div class="sandbox-warning">${Resource.msg('payment.error.general', 'globalpay', null)}</div>
</isif>
        <isinclude template="checkout/billing/paymentOptions/uniounPayForm" />
    </fieldset>
</fieldset>
  </div>
